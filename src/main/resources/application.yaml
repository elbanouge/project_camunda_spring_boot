spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/camunda-database
    username: postgres
    password: admin

camunda.bpm.admin-user:
  id: admin
  password: admin

server:
  port: 8090